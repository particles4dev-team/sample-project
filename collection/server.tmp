{{ name }} = new Meteor.Collection('{{ name }}',{
    transform: function(doc) {return new {{ name }}Document(doc);}
});

{{ name }}.deny({
    insert: function() {return true;},
    update: function() {return true;},
    remove: function() {return true;}
});

{{ name }}Document = function(doc){
    var self = this;
    _.extend(self, doc);
};
_.extend({{ name }}Document.prototype, {
    constructor: {{ name }}Document
});